# Sin URL ni credenciales de BD aquí: van en backend/.env (ver env.example en la raíz del repo).

#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true

#server.port=${SERVER_PORT:8080}

#gemini.api.key=${GEMINI_API_KEY:}
#gemini.model=${GEMINI_MODEL:gemini-2.5-flash}
#${JWT_SECRET:}
#${JWT_EXPIRATION_MS:86400000}
jwt.secret=visume-super-secret-key-2024-cambiame-en-produccion
jwt.expiration=86400000
spring.datasource.url=jdbc:mysql://localhost:3306/visume?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

#BD Mac
#spring.datasource.url=jdbc:mysql://localhost:8889/visume?useSSL=false&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=root
####################################

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.forward-headers-strategy=framework
# ===========================
# JPA / HIBERNATE CONFIGURATION
# ===========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===========================
# APPLICATION CONFIGURATION
# ===========================
server.port=8080

# ===========================
# GEMINI CONFIGURATION
# ===========================
#GEMINI_API_KEY
gemini.api.key=AIzaSyCh4e_67Z21h2juUmOjcyvIIM0Ar1YzPyY
gemini.model=gemini-2.5-flash

# ===========================
# JWT CONFIGURATION
# ===========================
#JWT_SECRET=
#JWT_EXPIRATION_MS=86400000


logging.level.com.visume=DEBUG
logging.level.org.springframework.web=DEBUG
